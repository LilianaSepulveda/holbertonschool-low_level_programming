#include "holberton.h"

/**
 * print_rev -  prints a string, in reverse, followed by a new line.
 * @s: String
 */
void print_rev(char *s)
{
	int r = 0;

	while (s[r++])
		rev++;

	for (r = r - 1; rev >= 0; rev--)
		_putchar(s[r]);

	_putchar('\n');
}
